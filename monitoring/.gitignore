# Generated monitoring artifacts.
# The HTML reports in reports/ + DRIFT_SUMMARY.md are committed as deliverables.
# The drifted datasets and request logs stay out of git (bulky and reproducible).
drifted/
logs/
__pycache__/
# Sampled rehearsal output from `anomaly_verification.py --scenario/--sample`. Not the
# canonical numbers, so it must never be mistaken for the committed reports above.
reports/demo/
